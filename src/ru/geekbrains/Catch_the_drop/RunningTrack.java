package ru.geekbrains.Catch_the_drop;
import java.util.Random;
public class RunningTrack {
    int l = (int) (Math.random() * 100);
    private int length = l;

    public int getRuntTrackLength() {
        return l;
    }
}
